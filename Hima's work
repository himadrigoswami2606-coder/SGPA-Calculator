<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>SGPA Calculator</title>
<style>
    body {
        font-family: 'Poppins', sans-serif;
        background: linear-gradient(135deg, #2980b9, #6dd5fa);
        color: #fff;
        text-align: center;
        padding: 20px;
    }
    h1 {
        font-size: 28px;
        margin-bottom: 20px;
    }
    .container {
        background: rgba(255,255,255,0.1);
        padding: 20px;
        border-radius: 15px;
        max-width: 600px;
        margin: auto;
        box-shadow: 0 4px 20px rgba(0,0,0,0.2);
    }
    input {
        padding: 10px;
        width: 80%;
        margin: 8px 0;
        border: none;
        border-radius: 8px;
    }
    button {
        background: #27ae60;
        color: #fff;
        padding: 10px 20px;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        font-size: 16px;
    }
    button:hover {
        background: #2ecc71;
    }
    .result {
        margin-top: 20px;
        font-size: 22px;
        font-weight: bold;
    }
</style>
</head>
<body>

<h1>🎓 SGPA Calculator</h1>

<div class="container">
    <p>Enter Subject Marks (out of 100) and Credits:</p>

    <div id="subjects"></div>

    <button onclick="addSubject()">+ Add Subject</button>
    <br><br>
    <button onclick="calculateSGPA()">Calculate SGPA</button>

    <div class="result" id="result"></div>
</div>

<script>
let subjectCount = 0;

function addSubject() {
    subjectCount++;
    const div = document.createElement('div');
    div.innerHTML = `
        <input type="text" id="sub${subjectCount}" placeholder="Subject Name">
        <input type="number" id="marks${subjectCount}" placeholder="Marks out of 100">
        <input type="number" id="credit${subjectCount}" placeholder="Credit">
        <br>
    `;
    document.getElementById('subjects').appendChild(div);
}

function getGradePoint(total) {
    if (total >= 90) return 10;
    else if (total >= 80) return 9;
    else if (total >= 70) return 8;
    else if (total >= 60) return 7;
    else if (total >= 50) return 6;
    else if (total >= 40) return 5;
    else return 0;
}

function calculateSGPA() {
    let totalPoints = 0, totalCredits = 0;

    for (let i = 1; i <= subjectCount; i++) {
        const marks = parseFloat(document.getElementById(`marks${i}`).value);
        const credit = parseFloat(document.getElementById(`credit${i}`).value);
        if (isNaN(marks) || isNaN(credit)) continue;
        const grade = getGradePoint(marks);
        totalPoints += grade * credit;
        totalCredits += credit;
    }

    if (totalCredits === 0) {
        document.getElementById("result").innerHTML = "Please enter valid data!";
        return;
    }

    const sgpa = (totalPoints / totalCredits).toFixed(2);
    document.getElementById("result").innerHTML = `Your SGPA = ${sgpa}`;
}
</script>

</body>
</html>
